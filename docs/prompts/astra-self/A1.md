# A1 offline operating prompt

Implement the user's offline A1 ruling on main without checkout, network, secrets, CLI installation, linking, pushing, or remote SQL execution. Preserve other lanes' work. Read the required spec, contracts, runtime facts, plan, task, skills, and installed Next/Supabase APIs before implementation.

## Files

- Create `supabase/migrations/0001_init.sql`, `0002_auth_hook.sql`, and `0003_integrity.sql` by extracting the exact A1 SQL fences; append `alter database postgres set app.invites_required = 'true';` to 0002.
- Create `supabase/config.toml` containing only `project_id = "brogram"`.
- Create `scripts/seed-load.mjs`: explicit snake_case column allowlists; strip `$` metadata; load courses/clos/patterns wrappers, top-level exercise files, and drill `items` wrappers; tolerate missing optional folders; stable crypto UUIDv5 exercise IDs; upsert patterns/id, courses/code, clos/id, exercises/id, drills/id. Dry run performs only reading, mapping, and count reporting.
- Replace the excluded placeholder `src/lib/supabase/server.ts` with exactly the two specified exports. Remove its exact `.git/info/exclude` entry if filesystem permissions permit.
- Create `docs/prompts/astra-self/A1.offline.test.mjs` for meaningful offline seed and auth boundary tests within the user's stage allowlist.
- Record verification, local API evidence, manual commands, and SQL review deltas in this directory. The shared build log and OpenSpec ledger already have Claude edits; leave them to Claude and report the status delta.

## Interfaces (copied from the plan)

- Produces: tables per spec §4; view `exercises_public`; functions `integrity_score(uuid)`, `hook_gate_signup(jsonb)`, `lift_expired_restriction()`; triggers; RLS policies.
- Produces in `src/lib/supabase/server.ts` (`import 'server-only'`): `getUserAndProfile(): Promise<{ user: User | null; profile: { id: string; account_status: AccountStatus; restricted_until: string | null } | null }>` using `getUser()`/`getClaims()`, never `getSession()`; `serviceClient(): SupabaseClient` with the service role key.
- Consumes: `seed/*.json`, `seed/exercises/*.json`, `seed/drills/*.json`.

## Verification

Run offline behavioral tests first to expose missing implementation, then after implementation. Compare migration contents against plan SQL fences plus the required trailing setting. Run `node scripts/seed-load.mjs --dry-run`, `npm.cmd test`, `npm.cmd run build`, and `npx.cmd tsc --noEmit` (after build). Record outputs and exit codes. No Supabase calls occur in dry-run or tests; network boundaries in tests are replaced locally.

Every git invocation sets GIT_CONFIG_COUNT=1, GIT_CONFIG_KEY_0=safe.directory, GIT_CONFIG_VALUE_0=C:/Users/musal/OneDrive/Desktop/V/Brogram. Stage only `supabase scripts/seed-load.mjs src/lib/supabase/server.ts docs/prompts/astra-self`. Commit directly on main with `feat(db): schema, rls, auth hook, integrity, seed loader, server helpers`. Retry an index.lock collision after five seconds.

## Three likely failures

1. Mapping the wrong wrapper/columns or recursing into unverified exercises produces wrong counts or invalid upserts; verify payloads, counts, dependency order, and repeatable UUIDs.
2. Trusting session data or sharing a service client with request cookies bypasses identity boundaries; verify getUser and the profile filter, null/error paths, cookie refresh, and nonpersistent service auth.
3. Concurrent lane changes or sandbox Git permissions interfere with staging/commit; inspect scoped diffs, never switch branches, never stage others' paths, and report permission failures accurately.

## Current plan corrections

- Current seeds have 95 top-level exercises. The Claude lane has an `unverified/` subfolder containing Java exercises; its contents are outside the requested top-level glob. Do not load nested files to force the older expected count of 110.
- A1 hook is read-only; invite redemption occurs in the auth.users bootstrap trigger, as the plan says. The older task file's claim of redemption during a direct hook call is obsolete.
- Preserve all three SQL blocks verbatim despite review findings; request a separate SQL correction for profile_guard versus SECURITY DEFINER integrity/expiry updates.

## Completion status

- [x] Exact migrations and minimal config written and compared with the plan.
- [x] Seed loader and both server helpers implemented; 11 offline tests pass.
- [x] Dry run, full repository tests, production build, and standalone TypeScript check passed.
- [ ] Remove the helper exclude entry and commit: Windows denied writing `.git/info/exclude` and `.git/index.lock`; staging retry after five seconds failed with the same permission error.
- [ ] CLI setup, link, push, real seed upload, hook registration, and live SQL/auth verification are pending for Musa as explicitly ruled. Exact commands and evidence are in `A1-handoff.md`.
