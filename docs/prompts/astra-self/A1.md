# A1 offline operating prompt

Apply the Claude lane's reviewed A1 hardening delta offline in the current working tree. Edit the unpushed migrations in place. No network, secrets, CLI installation, linking, pushing, SQL execution, staging, commits, or branch changes. Preserve other lanes' work. The user's delta supersedes the original plan's verbatim SQL and Git instructions.

## Files

Modify only these seven files; create no files:

- `supabase/migrations/0001_init.sql`: revoke all authenticated view privileges before granting SELECT, enable the view's security barrier, and fix search_path on its four SECURITY DEFINER functions.
- `supabase/migrations/0002_auth_hook.sql`: grant schema usage to supabase_auth_admin, fix the hook's search_path, and remove the database-level invite setting from the migration.
- `supabase/migrations/0003_integrity.sql`: split admin IDs with whitespace-tolerant comma delimiters, preserve a future restriction expiry, fix search_path, and qualify the account_status declaration.
- `scripts/seed-load.mjs`: ignore environment values blank after trim when merging credentials over .env.local.
- `docs/prompts/astra-self/A1.offline.test.mjs`: cover empty/whitespace environment fallback and retain nonblank override coverage with synthetic credentials and intercepted fetches.
- `docs/prompts/astra-self/A1-handoff.md`: record landed commits, manual invite setting and new-connection timing, privilege probes, and current offline evidence.
- `docs/prompts/astra-self/A1.md`: this operating prompt and completion record.

## Interfaces (copied from the plan)

- Produces: tables per spec §4; view `exercises_public`; functions `integrity_score(uuid)`, `hook_gate_signup(jsonb)`, `lift_expired_restriction()`; triggers; RLS policies.
- Produces in `src/lib/supabase/server.ts` (`import 'server-only'`): `getUserAndProfile(): Promise<{ user: User | null; profile: { id: string; account_status: AccountStatus; restricted_until: string | null } | null }>` using `getUser()`/`getClaims()`, never `getSession()`; `serviceClient(): SupabaseClient` with the service role key.
- Consumes: `seed/*.json`, `seed/exercises/*.json`, `seed/drills/*.json`.

## Verification

Write and run the credential regression tests before the loader fix, then run `node --test docs/prompts/astra-self/A1.offline.test.mjs` and `node scripts/seed-load.mjs --dry-run` after implementation. Re-read each entire migration for SQL/PLpgSQL syntax and the exact requested changes; obtain an independent static review. There is no local Postgres: all SQL remains unexecuted. Record commands, final output, and exit codes in the handoff. Tests use synthetic credentials and intercept fetch; dry run reads no credentials and opens no connection.

Use read-only Git inspection to verify the commit record and scoped diff. Do not stage, commit, edit .git/info/exclude, create branches, or switch branches; the Claude lane handles commits. Full application build/tests are outside this focused delta's requested verification.

## Three likely failures

1. Default view privileges still permit writes through owner rights, or one definer lacks a fixed search_path; check every grant and all six definer declarations, then document the live permission probe.
2. A repeated integrity event extends an existing future restriction, or the regex literal is misescaped; review the CASE branches and literal SQL backslashes carefully.
3. Empty environment values mask usable local credentials, or a regression test reaches the network; use synthetic file values and intercept the actual client's fetch boundary.

## Current plan corrections

- Current seeds have 95 top-level exercises. The Claude lane has an `unverified/` subfolder containing Java exercises; its contents are outside the requested top-level glob. Do not load nested files to force the older expected count of 110.
- A1 hook is read-only; invite redemption occurs in the auth.users bootstrap trigger, as the plan says. The older task file's claim of redemption during a direct hook call is obsolete.
- The profile guard already uses current_user, and the attempts/state policies already check is_not_banned. Preserve those prior review fixes. Apply this hardening delta to the unpushed migrations rather than preserving the original plan SQL verbatim.
- A1 landed as `6fc5e6e`; A2 subsequently landed as `e118855`. No .git/info/exclude edit is outstanding.

## Completion status

- [x] Original migrations, minimal config, seed loader, and both server helpers implemented.
- [x] Original A1 verification passed: 11 offline tests, dry run, full repository tests, production build, and standalone TypeScript check (historical evidence in the handoff).
- [x] A1 committed as `6fc5e6e`; A2 subsequently committed as `e118855`. No helper exclude cleanup remains.
- [x] Seven-file A1 hardening delta applied: 15 offline tests pass, seed dry run passes, and all three migrations re-read with independent static review. SQL is unexecuted; changes remain unstaged and uncommitted for the Claude lane.
- [ ] CLI setup, link, push, real seed upload, hook registration, and live SQL/auth verification are pending for Musa as explicitly ruled. Exact commands and evidence are in `A1-handoff.md`.
