# BroGram build log

One line per decision or lesson, with the reason. Appended as the build runs. Read at the start of every phase.

- 16:53 Doha: build started with about two hours to the 19:00 deadline, not the six the plan assumed; phases will be compressed and anything red is reported as red.
- 16:53 Doha: pre-flight not done at start: no .env.local, and gh, vercel, supabase, scoop absent from PATH. A0 kicked off anyway so scaffold and build land; Vercel and GitHub steps will block until Musa logs in.
- 16:53 Doha: openspec 1.12.0 already installed, so the OpenSpec ledger is used instead of the plan checkboxes.
- 16:58 Doha: A0 is Codex job task-mtog2ukh-p0jpqw. Until its git init lands, nothing may be written into this tree (it would be swept into the scaffold commit), so drill generation, the exercise-bank author phase, and the verifier script are running into the scratchpad and get copied in on claude/* branches afterwards.
- 16:58 Doha: `python` is a Windows Store alias here; the real interpreter is `py` (3.13.2). Verification agents use `py` or the pyodide npm package.
- 16:58 Doha: exercise-bank workflow split into an author run (no runtimes needed) and a later verify, merge, critique run, because verification needs node_modules from A0. Same agents and prompts as docs/workflows/exercise-bank-generation.js.
- 16:58 Doha: OpenSpec change brogram-launch created with skip_specs: true; the spec lives in docs/, the ledger is openspec/changes/brogram-launch/tasks.md.
- 17:03 Doha: first A0 rescue did nothing in 6 minutes: the Codex rescue wrapper adds a "stop if credentials are missing" gate and Codex applied it to the absent Vercel and GitHub logins. Lesson: every rescue prompt must say explicitly which missing secrets are NOT blockers and what to do instead. A0 re-dispatched with that ruling: scaffold, install, build, local commit on astra/A0; deploy and push wait for Musa's logins.
- 19:10 Doha: deadline missed. Root causes, in order: pre-flight not done (no logins, no .env.local); the Codex sandbox has no network (npm registry EACCES), so the Astra lane could not install anything; the A0 retry ran 21 minutes then failed. The Claude lane finished A0's local steps itself under a ruling (install, build, commit) because they are tooling, not product code.
- 19:10 Doha: Codex needs `[sandbox_workspace_write] network_access = true` in ~/.codex/config.toml for any Astra task that installs or deploys. Claude's tools were denied that edit by the permission classifier; Musa adds the two lines.
- 19:10 Doha: `typescript` pinned to ^5 in package.json; `latest` resolves to 7.0.2, which drops `transpileModule` (the verifier and the JS/TS runtime adapter need it) and is not what Next 16 is tested against.
- 19:10 Doha: the .git directory was created by the Codex sandbox user, so every git command from Claude runs with `safe.directory` set via GIT_CONFIG_* env vars; deleting it was denied. Musa can run `git config --global --add safe.directory C:/Users/musal/OneDrive/Desktop/V/Brogram` to make it permanent.
- 19:10 Doha: generated in the scratchpad while the tree was blocked and copied in: 144 drill items (6 kinds x 24, verified by execution or line-by-line reasoning), 105 authored exercises across 26 CLOs (execution verification pending), scripts/verify-exercise.mjs (tested: smoke 4 passed, 1 java unverified; negative case exits 1). mingo 7.2.4 has no updateObject export; the verifier uses updateMany from mingo/updater. Pyodide npm setStdin takes { stdin: fn }, not { string }.
- 19:18 Doha: Phase 0 closed locally: build clean, contracts guard green, 144 drills committed (main = c4365e6, 4367883, 69672c6). No preview: Vercel not logged in. C1 and C2 dispatched on Opus in the same tree with disjoint paths and path-scoped commits; bank verify, merge, critique workflow running on Sonnet. Codex write tasks are held until C1 and C2 land, because a branch switch in the shared tree would break the subagents mid-task.
- 21:51 Doha: the claude.ai session usage limit hit at about 19:20 and reset at 21:50; it killed C1 (nothing written), C2 (two test files written), and 20 of 27 bank-verification agents. Re-dispatched all three; the workflow resumes from its journal so the 7 verified CLO files are not re-run.
- 22:08 Doha: C2 landed (d3991d8, 68 tests). Ruling: applyPass returns { mastery, points } rather than a bare Mastery, because quality and hintCount feed only pointsForPass and the exercise loop needs both values from one call; A4 codes against that shape. Cost if wrong: one call-site change in useExerciseLoop.
- 22:16 Doha: C2 review found a Critical in the chain rule caused by the controller's own model (chain patterns = last `chain` entries of a lifetime-distinct `patternsPassed`; breaks once a pattern repeats after a fail). Ruling: contracts stay frozen; on every pass the passed pattern is moved to the END of `patternsPassed` (set unchanged, order chronological within the chain), chain advances only when the pattern is not in the current suffix, and `chain` is capped at `patternsPassed.length`. Cost if wrong: mastery rows written by the loop carry a reordered `patternsPassed`, which the spec allows ("distinct patterns passed, ever").
- 22:16 Doha: C2 deferred (ledger for the final review): attempts rows carry no difficulty or quality, so points recomputed on reload use difficulty 3 and quality 70 unless the caller enriches rows; exact recomputation needs `difficulty` and `quality` columns on attempts (pilot-week schema change, Astra). Minor: integrity window has no upper bound on `created_at`; fetchBank selects `*` and has no `.order()`; trim sort NaN on bad dates; unvalidated casts in toExercisePublic; `new Date()` inside applyPass/applyFail; streak day keys use the timestamp's own offset (UTC from Supabase); one integrity test passes a paste count in the score slot.
- 22:34 Doha: Task C2 complete (commits 69672c6..039595c, review clean after fix round 1). Note for A4 and the dashboard: `chain` is capped at patternsPassed.length, not 3, so UI clamps for display and branches on `closed`, never on `chain === 3`.
- 22:40 Doha: JSON mode needs no explicit option. @ai-sdk/deepseek 3.0.39 sets `response_format: { type: 'json_object' }` itself whenever the AI SDK asks for a json response format (its `supportsStructuredOutputs` is false, so it never sends json_schema); `generateObject`/`streamObject` with the agent's Zod schema is enough. The `ai` 7.0.93 names in the plan are all real: `maxOutputTokens`, `partialObjectStream`, `result.usage.outputTokens` (`generateObject` is marked deprecated in v7 in favour of `generateText` with `output`, but still shipped and used). Not proven live: DEEPSEEK_API_KEY is absent from this machine, so C1 step 7's live Profiler call is RED.
- 22:47 Doha: Codex cross-review of C2 works offline (the model reaches OpenAI; only the sandbox's commands lack network) once the companion is run with the safe.directory GIT_CONFIG env vars. It found three material issues the Opus reviewer missed: fetchBank must filter verified=true; compiled accountStatus must mirror the profile row (server is the authority) instead of re-escalating from old events; streaks must expire when the last activity is older than yesterday. All three sent back as fix round 2. Lesson: cross-review pays; keep both reviewers on every task.
- 22:27 Doha: Task C1 landed (5e717fb, 127 tests of its own, build clean). JSON mode needs no provider option: @ai-sdk/deepseek 3.0.39 sends response_format json_object itself. Live Profiler call red (no DEEPSEEK_API_KEY). Opus review and Codex cross-review dispatched in parallel.
- 22:29 Doha: Ruling: with no remote, no gh, and one shared working tree, every lane commits directly on main with path-scoped staging; reviews run on commit ranges. Branch switching in the shared tree would strand another agent's commits on the wrong branch. Cost if wrong: history is linear and un-PR'd; acceptable for a day-one repo. Codex dispatched on A1's offline parts (three migrations, seed loader with --dry-run, real server.ts); link and push wait for the CLI and secrets.
- 22:31 Doha: C3 committed: 95 exercises at top level (88 verified by execution, 7 reading kinds skipped by design), 15 Java code exercises parked in seed/exercises/unverified/INFS3102.json. Ruling: seed JSON gets no separate Opus review; execution verification plus the critique pass is the review. Pilot-week queue: re-run docs/workflows/exercise-bank-generation.js with perPattern 3 and JUDGE0_API_KEY set, move verified Java rows out of unverified/, then npm run seed:load.
- 22:31 Doha: C4 closed: drills in 69672c6; profiler-fallback.json shipped inside C1.
- 22:31 Doha: C2 fix round 2 landed (451b001). Note for UI: accountStatus mirrors the server row, so gate on accountStatus never on integrityScore; streak expiry compares UTC date keys against the browser clock by default (pass a server now for exactness).
- 22:34 Doha: shadcn initialised (style base-nova, neutral, lucide) and the plan's component set added; committed. Capacity valve triggered (spec section 16): A5a has not started and Codex is serial and offline, so three Sonnet implementers build the six drill components, the report pages, and the admin tables as props-driven presentation under src/components/{derot,report,admin}; Astra wires them to data and reviews them.
- 22:36 Doha: Task C2 complete (commits 69672c6..451b001 within src/lib/learner, both reviews clean). Wiring notes for A2 and A4: pass a server-derived now to compileLearnerState; gate UI on accountStatus; attach the Diagnoser's mistakeLabel when writing the attempt so recentMistakes fills.
- 22:39 Doha: Opus review of C1 confirms prompts and schemas are byte-identical to the specs (machine-checked by prompts.test.ts) and agrees with Codex on the unexecuted agent_usage insert. Its extra findings queue for fix round 2: Profiler phase-2 fallback indexes by total answers (must filter to phase-2 ids); no mocked-provider tests for retry, fallback, Author insert, and real streaming; unknown parentExerciseId should 400 like reviewer/diagnoser; planner fallback with empty clos violates its own schema; partial-stream error can leave result.object rejecting unobserved; buddy summary should be the lowest three CLOs regardless of closed. Deferred minors: cacheHitTokens hard-coded 0; a 413 still burns a rate slot; buddy trim pops least-frequent label instead of oldest.
- 22:39 Doha: shadcn init left light tokens as default and the placeholder page lost contrast; ruling: `dark` class on the root html element (one word in src/app/layout.tsx, an Astra file, changed by the controller because the regression was the controller's shadcn commit).
- 22:39 Doha: verifier: Java single-file submissions must drop `public` from `class Solution` (Judge0 takes one file; the A3 adapter needs the same normalisation). Deferred to pilot week: the verifier's Python path is not interruptible; the mongo path executes the test's embedded args rather than the reference, so the reference is not certified for the four mongo exercises; A3 must define how the student's query substitutes into the test args, and the seed gets re-checked then.
- 22:47 Doha: C1 fix round 1 landed (06c64d0). Ruling corrected: the coach per-exercise cap is stored hint_count plus the in-memory count since the last attempt-failed (the implementer showed max() under-counts after a fail with a nonzero stored count). Round 2 dispatched with the Opus findings; ruling: Profiler answers are cumulative across phases and the fallback selects questions by counting answers per phase id set; planner requests require at least one CLO; unknown parentExerciseId is a 400.
- 22:49 Doha: A1 offline parts landed as 6fc5e6e (Codex wrote, Claude committed: the sandbox user gets Permission denied on .git/index.lock now that the index is owned by musal). Pending for Musa: scoop, supabase CLI, link, db push, seed load, hook registration.
- 22:49 Doha: Ruling on Codex's delta request: the plan's profile_guard tests auth.role(), which is still 'authenticated' inside the security-definer escalation and lift functions, so every escalation and every lift would raise. Fix: profile_guard tests current_user in ('authenticated','anon') instead (security definer runs as the owner; PostgREST sets the client role). Also add public.is_not_banned() to the attempts select policy and the learner_state policy per spec section 4. Both go to Astra as one delta with the Opus review findings.
- 22:50 Doha: valve admin tables landed (4874144, 26 tests) and report pages (ccf018e); Codex reviews the valve set once the drill components land.
- 22:51 Doha: A2 dispatched to Codex with the two ruled A1 corrections folded in (profile_guard on current_user; is_not_banned on attempts and learner_state policies), without waiting for the A1 Opus review, because Codex is serial and idle and the review's extra findings can ride the next delta.
- 22:54 Doha: C1 fix round 2 landed (0819a69; 350 tests project-wide, build clean). Deferred: usage under-counts prompt tokens when the first generateObject call throws (only the retry is billed).
- 22:57 Doha: valve drill components landed (8743575, 69 tests). All three valve pieces are in; Opus review dispatched on the set, Codex review follows with A2.
- 23:02 Doha: Opus review of A1: migrations byte-identical to the plan; loader and server helpers correct. Critical: Supabase default privileges give authenticated ALL on exercises_public, the view is auto-updatable and owner-rights, so UPDATE and DELETE would write through to exercises past RLS. Rulings for the A1 delta (next serial Codex task after A2): revoke all on the view from authenticated too, then grant select; set security_barrier on the view; grant usage on schema public to supabase_auth_admin; drop the alter database line from 0002 (owner-only on Supabase, and the coalesce default already fails closed) and move it to the manual SQL step; admin id list split on whitespace-tolerant commas; set search_path on the security-definer functions; restricted_until does not stack (a further event while restricted leaves the existing expiry); loader ignores blank env values; stale handoff text corrected; plan counts corrected (done by the controller: exercises 95, drills 144).
- 23:04 Doha: C1 re-review over rounds 1 and 2: all twelve findings addressed by source inspection and hand traces; round 3 dispatched for two tests lost in the round-2 reorganisation and a rate-slot leak on a thrown backstop.
- 23:07 Doha: Opus review of the valve set: drills need fixes (HoldFocus fixed height clips every seeded passage; NBack captures Space document-wide and restarts its token timer on parent re-render; predict-output normalisation collapsed newlines), report needs the PDF happy-path test, admin needs UTC-pinned dates and a flag for absent CLO-pattern pairs. Rulings: HoldFocus sizes to content with the void guards kept; output normalisation is per line with newlines significant. All three fix rounds dispatched in parallel. Deferred minors: unchecked payload casts, trace render throws on a missing expected key, aria labels and aria-sort, agent usage shows the last 14 present days not a calendar window, a Dialog per users row, trace grading requires exact literal spacing.

- A2 local completion: applied only the two authorized A1 corrections in 0001_init.sql (current_user in profile_guard; is_not_banned for attempts select and learner_state using/with check). Other A1 hardening from the 23:02 review remains queued for the next task.
- A2 installed APIs: Next 16.3.4 docs/01-app/03-api-reference/03-file-conventions/middleware.md deprecates middleware.ts in favor of proxy.ts; src/proxy.ts delegates to src/lib/supabase/middleware.ts. SSR 0.12.6 src/types.ts defines setAll(cookies, headers): refresh cookies AND cache-control/expires/pragma are preserved across redirects, and request cookies are refreshed for downstream components. Supabase JS 2.115.0 uses auth-js getClaims() with nullable data and verifyOtp({token_hash,type:'email'}).
- A2 account gates: Next cookies.md prohibits cookie mutation during Server Component rendering, so banned layout/proxy requests go through /auth/signout, which verifies the ban again and signs out using a cookie-writable route response before /login?reason=banned. It never signs out an active account and never claims success on a signOut error. Appeal line points to the Velocity invitation email; no unverified address was invented. Restricted exercise checks also run in proxy because layouts are reused across navigation; internal pathname header is always overwritten.
- A2 session and UI: one Zustand store per provider, with newer server revisions synchronized while newer local state is preserved. Saved learner state is hydrated intact; compileLearnerState supplies defaults only for a missing document (no fabricated history and no forbidden integrity_events reads). Dashboard fetches minimal metadata from courses/clos/exercises_public and never invokes an agent on mount. Buddy/wellness slots are ready for A5a. Root footer/dark class/Geist fonts retained; fixed the existing self-referencing --font-sans token. Base UI buttons rendered as links keep button semantics, so navigation uses Link plus buttonVariants. Next 16.3.4 error.md requires parent error boundaries for layout errors and retry() for server re-fetch, both implemented and tested.
- A2 verification: npm.cmd run build exit 0 (Next 16.3.4, all 10 pages generated, Proxy listed); npm.cmd test exit 0 (48 files, 415 tests, 11.33s, run at 23:06:53); scoped npx.cmd eslint exit 0. Red/green tests cover confirmation redirects/errors, verbatim login hook errors, layout banned/restricted branches, cookie retention/forwarding, session isolation/refresh, dashboard order/empty/retry/stale responses, and error retry. Existing Next ancestor-lockfile and Vite tsconfigPaths warnings remain informational.
- A2 production smoke: npm.cmd run start -- --hostname 127.0.0.1 --port 3102, then local Node fetch and Playwright using installed Chrome. /dashboard without configuration returns 307 to /login?error=configuration. Desktop login at 1280x800 and mobile login/ban notice at 390x844 have no horizontal overflow; footer/contact line present, banned form hidden, no browser errors. Screenshot inspected at .next/a2-login.png (ignored build artifact).
- A2 pending for Musa: apply migrations/register hook and configure Supabase email template/redirect allow-list; real signed-out /dashboard redirect; real uninvited Gmail hook message via signInWithOtp; restricted and banned gates with a real user (reset statuses afterward); preview deployment URL. No preview was deployed and no git stage/commit/branch/push was attempted; files remain in the shared working tree for Claude to commit.
- 23:12 Doha: Task C1 complete (5e717fb..80244f0 within src/lib/agents and src/app/api, three fix rounds, both reviews clean). A2 landed as e118855 (Codex wrote, Claude committed; Next 16 uses src/proxy.ts instead of middleware.ts; banned sign-out is a route handler because layouts cannot clear cookies). Opus review of A2 dispatched; A1 hardening delta running in Codex.
- 23:15 Doha: valve fix rounds landed: drills 277a3fa (76 tests), admin a608c06 (30 tests; an index race swept the report lane's PDF test into this commit, harmless). Scoped re-review dispatched.
- 23:20 Doha: valve set clean after fix round 1 (drills 277a3fa, admin a608c06 which also carries the report's PDF test, marker 01c0982). Forward dependency for the wiring lane: CloRef.patterns is required, supply it from clos. Codex review of the whole range runs after the A1 hardening delta lands so the working tree is quiet.
- 23:22 Doha: Opus review of A2: approved; Next 16 proxy convention, retry prop, cookie limits, and the self-referential --font-sans fix all verified against node_modules. Delta queued for the A4 dispatch: guard row.state shape before spreading into LearnerState (fall back to compileLearnerState); forward account_status and restricted_until from the proxy in internal headers so the layout stops re-reading profiles through getUser; filter draft CLOs from the mastery grid; open-source link beside the footer; redirect a signed-in visitor from /login to /dashboard; unify on the semantic tokens. Minor: /auth/signout is a GET (bounded blast radius, only signs out a verified banned profile).
- 23:29 Doha: A1 hardening delta landed as a3727ba (offline tests 15 passed; dry run 95 exercises, 144 drills). Codex range review since 3ec9d29: five P2s, all valid: profiler fallback must count live p1q ids (C1 round 4 sent); paste-in-exercise count needs the 7-day window (SQL, prefaced to the A3 dispatch); hydrated streaks never expire in the (app) layout (queued with the A2 delta for A4); report double-counts diagnosed failures and jsPDF needs hotfixes px_scaling for true A4 (report round 2 sent). A3 dispatched to Codex with the mongo substitution convention and the Java single-file normalisation ruled.
- 23:31 Doha: C1 round 4 landed (0faaa4f): profiler fallback counts questions by id prefix (p1 and p2), live or fixture. Scoped check folded into the next re-review.
- 23:37 Doha: re-review of C1 round 4 and report round 2: all addressed, no breakage. Claude-lane code is closed for this session's scope (C0 to C4); C3b, C5, C6 remain blocked on Supabase, the exercise screen, and Musa.

- A3 SQL review delta: `0003_integrity.sql` now filters the per-exercise paste count with the same strict seven-day window as integrity_score. This edits the local migration only; no remote SQL was applied.
- A3 implementation: getRuntime routes all seven languages to six adapters; Python, JS/TS, SQL and Mongo share one active/standby worker controller with per-test deadlines capped at 5 seconds, package preparation on both slots, remaining-test timeout results, abort during preparation, and recovery after an idle worker crash. Web uses allow-scripts-only opaque-origin iframes, fresh DOM per test, source/nonce validation and timeout disposal. Java uses one normalized Main.java source, the existing auth/rate helpers and guarded Judge0 POST/fallback polling. No frozen contracts, other-lane source, dependencies, secrets or git metadata were edited.
- A3 installed APIs checked: Next 16.3.4 installed route-handlers.md confirms standard Request/Response POST and Node runtime. Pyodide 314.0.6 pyodide.d.ts confirms setStdin({ stdin }), loadPackage(names,{messageCallback}), runPython({globals}) and PyProxy.destroy. Browser Python imports the pinned v314.0.6 CDN script from a classic worker; offline tests load the installed npm package in Node. TypeScript 5.9.3 types confirm transpileModule/diagnostics and AST modifier spans; JS export removal uses AST spans so literals/comments survive. sql.js 1.14.2 package browser export is dist/sql-wasm-browser.js without fs/path; Node tests use its local WASM. mingo 7.2.4 top-level types expose find/aggregate/updateMany with registered operators. jsdom 30.0.1 executes the real web bootstrap in tests; only iframe srcdoc/postMessage transport is shimmed.
- A3 conventions: SQL reloads fixture per test, supports __STUDENT__ and after DML, and infers schema setup when every test lacks placeholders because RunRequest has no kind. Mongo nonempty args stay fixed per the user ruling, empty/sentinel args use a JSON query/pipeline/update pair; this also preserves the current seed's shell-style reference strings for fixed checks. Python infers function mode from JSON arrays and the last top-level def, with optional # brogram:stdin override, because RunRequest has no mode/fnName. subscribeRuntimeProgress exposes actual package names without altering RuntimeAdapter. Judge response adds failureKind to preserve compile/timeout/runtime status distinctions; absent key produces one actionable failed test.
- A3 verification: targeted runtime/judge tests 81/81 green; npm.cmd test exit 0 at 23:44:58 (57 files, 514 tests, 9.35s), including all five smoke exercises with reference code, TS/Mongo goldens, 10 SQL code/schema bank references, four Mongo seed references, judge 401/413/503, status mapping/polling/abort, and worker timeout/promotion regressions. npx.cmd eslint src/lib/runtimes src/app/api/judge exits 0. npm.cmd run build exits 0 (Next 16.3.4, 11/11 pages, /api/judge listed). Two additional builds with temporary src/app/runtime-build-check/page.tsx forced every browser worker dependency into Turbopack; compiled Python CDN loader, TypeScript and SQL WASM browser chunks verified. That temporary entry was removed before the final test/build. Existing Vite tsconfigPaths and Next ancestor-lockfile warnings remain informational.
- A3 verification boundaries: the manual browser infinite-loop timing check, including the 500 ms standby start target, is still pending for Musa and was not performed by Astra. Automated lifecycle tests use controlled transport and fake timers; web iframes may share the browser event loop, so parent-timer disposal does not establish a hard cutoff for a synchronous DOM-script loop. CDN/package downloads and live Judge0 were not exercised. No stage, commit, branch switch, push, preview deploy or remote migration was attempted; Claude owns committing these working-tree files.
- 23:48 Doha: A3 landed as 9fb6216 (Codex wrote, Claude committed; 81 runtime and judge tests, 514 project-wide, build clean). Opus review dispatched. A4 dispatched to Codex with the A2 delta and the streak-expiry-on-hydration item.
- 23:59 Doha: Opus review of A3: timeout and standby machinery, judge route, and SQL delta correct and tested against real engines. Critical: pyodide-engine picks the LAST top-level def as the entry point; the seed's 'Discount then tax' (entry first, helpers after) grades red in the browser but green offline. Ruling for the A3 delta (next Codex task after A4): pick the def whose arity matches the test's argument count, first such def, else the first def; regression test on that exercise; judge adapter floors its client deadline at 30 s (timeoutMs is a browser-runtime number); web adapter clears a dead frame and abort() is a no-op with nothing pending; judge route writes an agent_usage row (agent judge) so the hourly backstop counts across instances; totalCount from req.tests.length on the transport-failure path; delete the empty runtime-build-check directory. Minor deferred: program-mode actual rewritten to expected on pass; the JS worker imports the whole TypeScript compiler for plain JS; web.ts relative contracts import.

- A4 / A2 review delta (2026-09-06): guarded saved learner documents before spreading, passed saved state and the server clock into compileLearnerState, supplied paginated date-only attempt history plus drill dates, and preserved cached aggregates/last dates. Profile access is checked once in the proxy after lifting restrictions and forwarded in overwritten internal headers; app layout retains getUser for identity. Ordinary authenticated login redirects to dashboard; account-unavailable recovery remains reachable to prevent a redirect loop. Draft CLOs are filtered, footer has Open source, and dashboard/shell use semantic neutral tokens.
- A4 implementation: stationary prompt/editor/results workspace, collapsed wellness strip, CodeMirror language extensions and clipboard/right-mousedown guards, opaque blur/PrintScreen cover plus 15-second idle cover, one idle event after 60 seconds, per-type batched integrity writes. Separate pointer-enabled overlay covers inert work controls. Predict-output, clickable bug lines, trace cells and SQL schema editor use the task's grading conventions. Hidden expected/actual/names/output are withheld from UI, and malformed streaming partials cannot crash rendering.
- A4 loop: imports frozen runtime/agent/learner contracts, calls no agents on mount/input/timer/free run, writes durable attempts before diagnosis/review, commits terminal envelopes only, appends mistakeLabel, checkpoints retries, applies returned mastery/points and branches on closed. First hint after each new failure unlocks on the first edit (even if undone) or 60 seconds; later hints for that failure wait 60 seconds from Coach; max five per exercise. Per-user/exercise receipts preserve quota across reload, and brogram:attempt-active is available to A5 wellness. Attempt duration restarts after each submission. Bank selection starts at difficulty 3, permits mixed languages on a CLO, validates generated reference execution and the verify response, and strips the reference before UI state.
- A4 installed API evidence: Next docs headers.md/use-params.md/layouts-and-pages.md confirm async headers, dynamic useParams and retained layouts. CodeMirror installed types confirm Compartment.reconfigure, readOnly/editable, domEventHandlers/contentAttributes and SQLite dialect. Existing subscribeRuntimeProgress exposes packageName/message without changing RuntimeAdapter. Postgrest installed declarations confirm range(from,to) for full date-history hydration. Playwright test.d.ts confirms config/webServer and browser cookie setup; Supabase auth types confirm generateLink (no email send) and verifyOtp. No dependencies added and no font/env mocks were needed for the build.
- A4 final verification: npm.cmd test exit 0 at 00:17:05 Doha, 63 files / 574 tests passed, 12.62s. Required agent-trigger spy and fake-clock lockdown tests pass; additional regressions cover streaming, all answer kinds, hidden-output protection, Author verify/fallback, mixed-language chains, hint reload/cooldown, retry deduplication, stale mastery protection and history pagination. npm.cmd run build exit 0: Next 16.3.4, 11/11 static pages generated, /exercise/[id] dynamic route and Proxy listed. Scoped npx.cmd eslint exits 0; npx.cmd tsc --noEmit --pretty false exits 0. Test/build logs are local ignored artifacts at test-results/a4-vitest.log and test-results/a4-build.log. Existing Next ancestor-lockfile and Vitest configuration/performance notices remain informational.
- A4 C5 handoff: playwright.config.ts and e2e/fail-fix-pass.spec.ts collect successfully (npx.cmd playwright test --list: 1 test in 1 file). The spec creates/cleans a temporary invited account, mints its session without email, uses the seeded JavaScript smoke reference through the real browser runtime, and checks fail/fix-plan/hint/pass/different-pattern plus stored attempts. Actual browser execution and node scripts/seed-load.mjs counts remain PENDING for Claude C5 with configured Supabase; neither was run here. An external PLAYWRIGHT_BASE_URL target must also use AGENT_DRY_RUN=true.
- A4 deviations and Claude deltas: a CLO with fewer than two distinct bank examples uses nearest-course fallback instead of inventing Author examples. The frozen AuthorReply omits the wire ID, so the consumer validates it locally. Claude's Author schema still requires 5-8 tests for all kinds; make it kind-aware for the task's single-answer predict-output/spot-the-bug/trace shapes. Attempt/state/mastery writes are separate: older retries facing newer cross-tab progress stop with an explicit recovery error instead of re-awarding or overwriting; an atomic server operation would remove that remaining reconciliation limit. Frozen agents/learner/contracts/runtimes, seed, derot/report/admin and docs/contracts were not edited. No git staging, commits, branch switches, pushes, or remote writes were performed; Claude commits these working-tree changes.
