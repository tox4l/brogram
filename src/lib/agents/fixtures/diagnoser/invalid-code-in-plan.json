{
  "intent": "You were going for a loop that stops at the first late train and reports it.",
  "rootCause": "The loop returns `-1` inside the loop body, so it exits on the first element that is not late.",
  "mistakeLabel": "return inside loop body",
  "fixPlan": [
    "Trace the loop by hand with the first visible example and note exactly when the return runs.",
    "Rewrite the ending like this:\n```python\nreturn -1\n```",
    "Check the empty-list example and decide what the function should give back when there is nothing to check.",
    "Run the visible examples before you submit again."
  ]
}
