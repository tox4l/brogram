# Source material that must never reach the public repo (contains instructor names and institution documents)
UDST START*/
UDST START*
BroGram.pdf

# Research scratch (kept locally, not published)
docs/research/raw-condensed.txt
docs/research/review-findings.txt
docs/research/condense-review.mjs

# Node / Next
node_modules/
.next/
out/
.vercel
*.tsbuildinfo
next-env.d.ts
/brogram-scaffold/
/.a0-artifacts/
npm-debug.log*

# Env
.env
.env*.local
.env*
!.env.example

# Supabase
supabase/.temp
supabase/.branches

# Test artifacts
test-results/
playwright-report/
coverage/

# CheerpJ: GPLv2+CE OpenJDK 8 tools.jar (18 MB), self-hosted, not committed to an
# MIT history. scripts/fetch-java-tools.mjs downloads it; the notice that must
# travel with it is public/java/TOOLS-JAR-LICENSE.md.
public/spikes/cheerpj/tools.jar
public/java/tools.jar
public/java/tools.jar.download

# OS
.DS_Store
Thumbs.db

# Leftovers from the first scaffold attempt (Codex sandbox could not delete them)
brogram-scaffold/
.a0-artifacts/

# Spike leftovers and build-fetched Java runtime. tools.jar is fetched by
# scripts/fetch-java-tools.mjs at prebuild; the notice that travels with it is
# public/java/TOOLS-JAR-LICENSE.md and stays tracked.
public/spikes/
# e2e/spikes/cheerpj.spec.ts still references public/spikes/cheerpj/index.html,
# now removed from git; the spec is testIgnore-excluded and stays as a manual-run
# historical record — see docs/build-log.md 22:33 for the spike's GO result.
# Generated by prebuild from seed/. Never hand-edited, never a source of truth.
public/curriculum/
# Local source clips for the sound sprite; the sprite itself is committed.
assets/sounds-src/
